// #define SUM(a, b) ((a) + (b))


// #include <stdio.h>

// #define SUM(a, b) ((a) + (b))

// int main() {
//     int x = 5;
//     int y = 10;
//     int result = SUM(x, y);
//     printf("The sum of %d and %d is %d\n", x, y, result);
//     return 0;
// }




// #define MAX(a, b) ((a) > (b) ? (a) : (b))



#include <stdio.h>

#define MAX(a, b) ((a) > (b) ? (a) : (b))

int main() {
    int x = 5;
    int y = 10;
    int max = MAX(x, y);
    printf("The maximum of %d and %d is %d\n", x, y, max);
    return 0;
}





