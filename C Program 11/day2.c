#include <stdio.h>

int main()
{

    // DataTye
    // Integer = > int => 1,2,3,4,5,6,67

    // Variable  => Identifier

    // Identifier = > Rules

    // Rule :

    // 1. 123name - > worng
    // correct = > name

    // 2. @name, #name -> worng

    //     correct - > name

    //     $name  name$  device_name
    // 3. 32 keywords reserved => You can't use reserved keyword as a identifier/variable name

    // 4. device name ->  worng

    //      correct -> device_name

    // int num = 10;
    int num;
    num = 20;

    printf("Value of Num is :   %d", num);
}